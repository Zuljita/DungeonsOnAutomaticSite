Single-line mono input. Pair with `Field` for a label; pass `invalid` to show the error state.

```jsx
<TextInput placeholder="12345" defaultValue="8F1A-7C2D" />
<TextInput type="number" min={2} max={8} defaultValue={4} />
```
